# 2026-03-31 — Daily Notes

## X/Twitter OAuth Reauthorization (Complete)

**Problem:** X API credentials expired, needed reauthorization.

**Root Cause:** Access tokens expired or revoked (common when X detects activity or password changes).

**Solution Process:**
1. Lothar went to X Developer Portal (developer.x.com)
2. Found two apps: "2027621167771627520lothareckst" (OLD) and "OpenclawX Integration" (NEW)
3. Initially confused which app to use — `.env` had Consumer Key from the OLD app
4. Regenerated BOTH Consumer Key AND Access Token (accidentally — should have only regenerated Access Token)
5. Updated `~/.openclaw/.env` with new credentials:
   - `X_CONSUMER_KEY=1fTnE6owG1rkUsRK9wbt51oo0`
   - `X_CONSUMER_SECRET=Lk2hPbvNulEFFPRCcfjG7181xR0rcXs19LTclE5YmYNbgZr`
   - `X_ACCESS_TOKEN=13020562-omyxF2iACDJCsrSEZtAw9klHouoh7QJMD3RcjyK9C`
   - `X_ACCESS_TOKEN_SECRET=ACPgsGpZtWNs0lndvL20RlVdbhtosiMUXMQPjmjJKflp6`

**Confusion Points:**
- File location: `.env` exists in THREE places on Felix MacBook:
  - `/Users/lothareckstein/.openclaw/.env` (OpenClaw uses this)
  - `/Users/lothareckstein/.env` (backup/source)
  - `/Users/lothareckstein/.fzshrc/.env` (unrelated)
- Editor caching: Pico/vim showed old values even after file was updated (cached view)
- Manual verification via `grep` confirmed tokens were saved correctly

**Fix Complete:**
- Tokens updated in `~/.openclaw/.env` on 2026-03-30 (I did it via edit tool)
- Tokens verified again on 2026-03-31 (Lothar manually checked, saw old values in editor, but `grep` showed new ones)
- Gateway restarted successfully (pid 37251)
- Test tweet drafted, awaiting approval

**Lesson:**
- Text editors can show stale cached views — always verify with `cat` or `grep`
- When reauthorizing X: only regenerate Access Token, NOT Consumer Key (unless switching apps)
- `.env` file is critical — contains ALL API credentials (X, Meta, Withings, Shopify, Anthropic, Brevo, etc.)

**Status:** ✅ Complete, awaiting test tweet approval to verify end-to-end.
