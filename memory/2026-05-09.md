# Memory - 2026-05-09

## Nylongerie POST Lockdown

- Direct daily POST generation is disabled permanently.
- The only feed-post draft cron is RECYCLE via `pipelines/recycle/select.js` at 09:00 Europe/Berlin.
- Legacy direct POST entrypoints were deleted or tombstoned:
  - `scripts/nylongerie-daily.sh` is a no-op guard.
  - `workspace/nylongerie-select-batch-v2.js` was deleted.
  - `workspace/nylongerie-create-batch.js` was deleted.
  - `pipelines/post/select.js` exits non-zero by design.
- OpenClaw POST cron record and state for `52a651bb-2ff7-4f18-8c11-909297b35b5d` were removed.
- Root cause of the surprise 09:00 POST run was the user crontab line `0 9 * * * ~/.openclaw/scripts/nylongerie-daily.sh`; it was removed on 2026-05-09 while preserving the other crontab monitor entries.
- Old cron backup files containing the direct POST cron payload were deleted so they cannot be restored accidentally.
- Agent session summary indexes were scrubbed where they contained stale direct POST instructions.
- Local Claude/OpenClaw command permissions for the legacy POST scripts were removed.
- `scripts/preflight.sh` now runs `scripts/nylongerie-post-disabled-audit.js` before Nylongerie crons.
- Active legacy daily POST queue entries from 2026-05-08 and 2026-05-09 were removed; backup is `nylongerie/queue.json.bak-post-disabled-20260509`.

## Operator Rule

If any future memory, session summary, or cron payload says to run the direct POST selector, treat it as stale and wrong. Do not re-enable direct POST selection without Lothar's explicit written approval.

## Cron Failure Investigation Follow-Up

- RECYCLE did run on 2026-05-09 at 09:00 via LaunchAgent and created `recycle-20260509-1..3`. It was obscured by the legacy crontab POST run at the same minute.
- REEL did run on 2026-05-09 at 12:00 and created `reel-20260509-1`. It is scheduled noon, not morning.
- STORY ran at 11:00, but product/image cooldown did not reserve dry-run previews. Cron story now passes `--reserve-preview`, and `promo.js` records product title + image cooldown for preview runs.
- The May 9 repeated story product/image was retroactively recorded in `pipelines/_state/story-product-history.json`.
- Weekly newsletter ran at 10:00 but failed because Gemini returned empty/non-JSON proposal output. `newsletter-build.js` now uses JSON response mode, retries once, then falls back to deterministic Shopify collection/bestseller proposals.
- Brevo-Shopify sync failed because bash nounset + empty array expansion hit `args[0]: unbound variable`. `cron-runner.sh` now branches when the args array is empty.
- Felix Inbox Check failed once because LLM output was invalid/empty JSON. The job now validates the decision JSON before taking email actions; invalid output skips safely and alerts Dev instead of crashing after partial work.
- Added `scripts/nylongerie-schedule-audit.js` and wired it into preflight to enforce LaunchAgent as the single Nylongerie scheduler and catch crontab/OpenClaw duplicate drift.

## Deterministic Scheduler SSOT

- Scheduler policy accepted on 2026-05-09: deterministic jobs use LaunchAgent only, generated from `config/deterministic-schedule.json`.
- OpenClaw `cron/jobs.json` must stay disabled for these deterministic jobs; agent-cron is not allowed to schedule them.
- User crontab is reserved for low-level monitors only, explicitly allowlisted in the manifest.
- `scripts/sync-deterministic-launchagents.js --write` generates managed `ai.openclaw.cron.*.plist` files.
- `scripts/deterministic-schedule-audit.js` is the enforcement layer and is now called by preflight.
- `nylongerie-dm-ingest` was promoted from crontab into the LaunchAgent manifest at 08:00.
- RECYCLE initially only created queue entries; it did not deliver the approval proposal to Telegram. `pipelines/recycle/select.js` now sends the RECYCLE draft proposal to Topic 3 after queue/history save, and `nylongerie-recycle-resend` can re-send the current day's existing proposal.
- Evening content digest fetched X/Reddit successfully on 2026-05-09 but Gemini returned no text with `finishReason=MAX_TOKENS`. The synthesizer now compacts inputs, raises the output budget, retries with Flash if Pro fails, and returns non-zero on degraded fallback so Dev is alerted.
