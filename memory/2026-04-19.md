# 2026-04-19 Daily Notes

## Pipeline Rebuild — 90-Day Blocking Fix

### Problem
Same images posted repeatedly on same Instagram accounts (2-4×). The `used-images.json` existed but:
- Was never actually checked during selection (v2 script only checked queue.json)
- Had duplicate entries (IMG_0979 appeared 5× with conflicting accounts)
- Account names inconsistent (@-prefixed vs plain)
- Publish script never updated used-images.json after posting

### What Was Built

**`nylongerie-cleanup-used-images.js`** — one-time cleanup:
- Normalized account names (stripped @, deduplicated)
- Backfilled 16 missing entries from queue.json
- Updated 39 ig_post_ids from published queue entries
- Archived old file to `archived-used-images-backup/used-images-backup-2026-04-19.json`

**`nylongerie-select-v3.js`** — selection with proper blocking:
- Loads used-images.json → builds blockedMap (filename → Set of blocked accounts)
- Per-account blocking: image blocked for Account A only if within 90 days AND used on Account A
- Cross-account allowed: same image on different accounts = fine
- Updates used-images.json IMMEDIATELY on selection (status: "selected")

**`nylongerie-publish.js`** — now updates used-images.json:
- After successful publish: updates status to "published", records ig_post_id
- Ensures no double-selection if cron runs again before approval

### Current State
- `used-images.json`: 91 images tracked, 91 actively blocked (all within 90-day window)
- Cross-account usage: 6 images used on multiple accounts (allowed, working correctly)
- `classify-results-clean.json`: 438 images (only active classify file now)
- Old classify files → archived/

### Archive Cleanup Done
- `classify-results.json` (2.4K entries) → archived/
- `classify-results-NEW.json`, `classify-results-v2.json` → archived/
- `queue.json.bak-bad-drafts` → archived/
- Daily logs Apr 5-14 → compressed (.gz)
- Classify/reclassify/verification logs → archived/

## Cron Status
- **Nylongerie Daily POST (10:00)** — disabled (was running via heartbeat during outage)
- **Morning briefing (7:30)** — working
- **Token refresh (every 2h)** — working (repaired after heartbeat-run period)
- **Daily pipeline tomorrow** — will use v3 selector with proper blocking

## Model Repetition Issue
Noted in morning: @libaniasilva12, @karolinamalisova, @_._s_lie, @arinaeagle repeating across days. With v3 blocking (90-day same-account), this should naturally diversify as the pool rotates.

## Watchdog Alert Context
Lothar received "Felix Watchdog: Gateway is DOWN" message — NOT from Felix. Source unclear (external monitoring?). Gateway was UP when checked. No action needed.
### REEL 90-Day Blocking Fix (13:29)
- `nylongerie-create-reel-batch.js`: Added same 90-day same-account blocking logic that posts have
- Previously: `!used[e.file]` blocked reel forever if in used-images.json (no expiry)
- Now: checks `entry.type === 'reel'` + `entry.used_date` + 90-day expiry, routes to account
- `blockedReels[file]` built at selection time, applied in filter
- Syntax verified ✅
