# Memory - 2026-05-08

## Deterministic Cron Handoff

- Source of truth is LaunchAgent + `~/.openclaw/scripts/cron-runner.sh`.
- OpenClaw `cron/jobs.json` agent crons are intentionally disabled to avoid duplicate runs and session drift.
- Active Nylongerie jobs confirmed loaded: RECYCLE 09:00, STORY 11:00, REEL 12:00.
- No `nylongerie-post` LaunchAgent is loaded. The pre-approved-pool POST pipeline remains off; daily POSTS currently come through RECYCLE.

## Model Routing

- Default execution model: `google/gemini-2.5-flash`.
- Premium reasoning model: `google/gemini-2.5-pro`, only for real synthesis/analysis slices such as newsletter topic selection/content writing, X/Reddit/content intelligence, and system review reasoning.
- Anthropic models are not allowed as fallback or default.

## Nylongerie Pipeline Fixes

- RECYCLE is active as the daily POST source via `pipelines/recycle/select.js`.
- REEL selector now uses classified `type:"reel"` entries when present, otherwise a deterministic local fallback video pool from `~/Desktop/nylongerie-content/archive/videos-unassigned.json` and `~/Desktop/nylongerie-content/inbox`.
- REEL duration is enforced with `ffprobe` at 3-90 seconds. Last verification found 253 fallback videos and 248 eligible candidates.
- REEL credit no longer guesses model handles from CDN-hash filenames. Use explicit `@handle` if present, otherwise neutral `@nylongerie`.
- STORY selector now tracks both Shopify product and Shopify image usage in `pipelines/_state/story-product-history.json`; `same_image_cooldown_days=90`.
- STORY dry-run still uploads/sends preview to Telegram Topic 3 but does not write cooldown state.

## False Cron Failure Alerts

- Screenshots at 10:43/11:05 showed `Felix deterministic cron failed` even though logs ended with `status=0`.
- Root cause: `scripts/cron-runner.sh` alerted every non-whitelisted job, regardless of zero exit status.
- Fix: alert only when `status != 0`; successful deterministic jobs stay quiet unless the job itself sends its normal preview/digest.
- `scripts/check-felix-health.sh` now reads deterministic `logs/cron-runner/*.latest.log` files instead of disabled OpenClaw `cron/jobs.json` state for Morning Briefing.
- Integration health also falsely reported WhatsApp disconnected because `integration_healthcheck.sh` expected old `openclaw channels list` wording (`linked`). OpenClaw 2026.4.x reports channels as `ON / OK` or `configured, enabled`. The check now accepts the current wording.
- Verified after patch: `integration-healthcheck` exits `status=0`, `/tmp/openclaw/health_alert.txt` is gone, and `openclaw status --all` reports Telegram `ON / OK` and WhatsApp `ON / OK`.
