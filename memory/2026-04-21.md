# 2026-04-21 Daily Notes

## Pipeline Status

### Morning Batch (09:00) ❌ FAILED
- **Error:** `Cannot find module '/Users/lothareckstein/.openclaw/workspace/nylongerie-select-batch-v2.js'`
- Pipeline failed at Step 1 (selection)
- No previews sent
- **Action needed: Fix script path**

### Promo Story (via promo-story.js) ✅
- Theme: Spring Collection
- Product: Glossy Transparent Thigh-High Stockings - 11 Colors
- Discount: 25% (SPRING25)
- DRY RUN → sent to Telegram Topic 3
- Template: sale

## 🚨 Broken Pipeline

**Root cause:** `nylongerie-select-batch-v2.js` was moved to `archived/` but the cron script (`~/.openclaw/scripts/nylongerie-daily.sh`) still references it at the old path.

**Fix options:**
1. Restore `nylongerie-select-batch-v2.js` from `archived/` to `~/.openclaw/workspace/`
2. Or update `nylongerie-daily.sh` to use `nylongerie-select-v3.js` from `~/.openclaw/nylongerie/`
3. Or consolidate: use just one selection script

**Current scripts:**
- `~/.openclaw/workspace/nylongerie-select-batch-v2.js` — MISSING (in archived/)
- `~/.openclaw/nylongerie/nylongerie-select-v3.js` — exists, active
- `~/.openclaw/scripts/nylongerie-daily.sh` — references missing file

**Also:** Promo story cron at `10 10 * * *` still uses `nylondarling` account but that account is exhausted. The script hardcodes `--account nylondarling`.